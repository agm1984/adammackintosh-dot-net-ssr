import React, { Component } from 'react'

// Use formula in iterative pattern
class Phi extends Component {
  render() {
    return (
      <div>
        phi = (1 + root 5)/2
      </div>
    );
  }
}

export default Phi
